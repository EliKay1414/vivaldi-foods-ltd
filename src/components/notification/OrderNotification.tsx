import React from 'react';

/**
 * Legacy OrderNotification component replaced by react-toastify
 * Toast notifications now appear top-center without interfering with the cart drawer.
 */
export const OrderNotification: React.FC = () => {
  return null;
};

export default OrderNotification;
